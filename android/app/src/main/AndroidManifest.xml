<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ShoppingListApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ShoppingList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Unused in production; hosts content for Compose UI tests (createComposeRule()'s
             default test host), which otherwise can't resolve an activity to launch. Debug builds
             already get this from the androidx.compose.ui:ui-test-manifest dependency (exported
             there), but that's debugImplementation-only, so release unit tests need it too. -->
        <activity android:name="androidx.activity.ComponentActivity" android:exported="true" />

        <!-- ShoppingListApp provides its own WorkManager Configuration (HiltWorkerFactory), which
             requires disabling WorkManager's default auto-initialization. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
